plugins {
    // Auto-provisions the JDK requested by the java toolchain when it is
    // not installed locally.
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'kc-nats-listener'
